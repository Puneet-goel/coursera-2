{
  "name": "fake-json-server",
  "version": "1.0.0",
  "description": 'npm install json-server -g\\\r json-server --watch db.json -p 3001 -d 2000.\\',
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
